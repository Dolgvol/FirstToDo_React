export const types = Object.freeze({
    All: 'all',
    Active: 'active',
    Archived: 'archived',
})

export const labels = Object.freeze({
    [types.All]: 'All Notes',
    [types.Active]: 'Active',
    [types.Archived]: 'Archived',
})
